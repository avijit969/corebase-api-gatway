import { Context } from "hono"
import { Bindings, Variables } from "../types"

const createRls = async (c: Context<{ Bindings: Bindings, Variables: Variables }>) => {

}

const updateRls = async (c: Context<{ Bindings: Bindings, Variables: Variables }>) => {

}

const deleteRls = async (c: Context<{ Bindings: Bindings, Variables: Variables }>) => {

}

export { createRls, updateRls, deleteRls }
